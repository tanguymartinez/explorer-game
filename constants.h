#ifndef constants_h
#define constants_h

const int TILES_WIDTH = 10;
const int TILES_HEIGHT = 5;
const int PIXEL_PER_TILE = 150;
const int WINDOWS_HEIGHT = PIXEL_PER_TILE*TILES_HEIGHT;
const int WINDOWS_WIDTH = PIXEL_PER_TILE*TILES_WIDTH;
const int FRAMERATE_LIMIT = 60;
const int FONT_SIZE = 24;

#endif
